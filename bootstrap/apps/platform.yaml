apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: https://github.com/idp-demo-corp/cluster-gitops.git
    path: platform/apps
    targetRevision: 29gc9
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated: 
      prune: true
      selfHeal: true